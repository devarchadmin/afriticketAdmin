'use client';
import ClientDetail from '@/app/components/apps/clients/ClientDetail';

const ClientDetailPage = ({ params }) => {
  return <ClientDetail params={params} />;
};

export default ClientDetailPage;